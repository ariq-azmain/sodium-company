import { Header, Summary, Wark, Contact, Faq, Members, Cards } from "@/section";

const Home = () => {
   return (
      <>
         <Header />
         <main>
            <Cards />
         </main>
      </>
   );
};

export default Home;
